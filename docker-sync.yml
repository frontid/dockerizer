version: "2"

syncs:
  "docker-sync-${PROJECT_NAME}":
    src: './'
    sync_args: '-prefer newer'
    sync_strategy: 'native_osx'
    sync_userid: '501'
    sync_groupid: '20'
    sync_excludes:
      - '.DS_Store'
      - '.git'
      - '.gitignore'
      - '.idea'