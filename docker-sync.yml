version: "2"

syncs:
  "docker-sync-${PROJECT_NAME}":
    src: './'
    sync_strategy: 'native_osx'
    sync_userid: '501:82'
    sync_excludes:
      - '.DS_Store'
      - '.git'
      - '.gitignore'
      - '.idea'
      - '.id'